**0.1**

В консоли невозможно прям одинаковой длины сделать, так как все элементы разные. Но приблизиться к одинаковому можно.
Вместо одного пробела | | я поставил два |  |, оно теперь выводится ровнее

**0.2**

Я решил, что у меня класс симуляция должен отвечать за изменение карты на один ход методом doMove.
Остальное - это задача не этого класса. У меня в классе Main делается ход каждые полсекунды, другого функционала, например, пауз нет. 
Сделать управление допустим через кнопку Enter следующий ход считаю избыточным функционалом, который я реализовывал в 1 проекте.
А реализацию пауз консоли через, допустим, Ctrl-C, как когда запускается Spring Boot через `mvn spring-boot:run` я пока не изучал и считаю это вне ООП, поэтому я пока что оставил только запуск симуляции.

### 1
Класс Position был написан очень давно, и я забыл убрать оттуда этот неиспользуемый метод, возвращающий int[] и вообще сделал класс record по подсказке IDE.

Про венгерскую нотацию и нейминг запомнил и буду стараться не применять её в будущем. Сейчас не вижу смысла всё исправлять, так как это будет только трата времени.
Разве что потренироваться писать нормальные названия. eatingList заменил на это entitiesWhichWasEated
Транслит я добавил когда пытался прописывать логику и забыл поменять на нормальное название. Со znakX я вообще забыл убрать метод, который я заменил на нормальный.
На счёт getMove() в идеале должен быть рекорд класс Move, который обозначает ход, и класс Анималс должен передавать только его, а не Position

### 2, 3
Тоже самое, учту в будущем, сейчас это только трата времени

### 4
О, а это важно. Не знал про то что так надо. Исправил один блок ловли ошибок, остальные не буду, главное что я запомнил как правильнее ловить ошибки.
```
}catch (RuntimeException e) {
log.error("Не удалось найти {}", stringName);
return 0;
}
```

### 5
Запомнил, в будущем буду стараться больше использовать printf. В коде исправлять не вижу смысла.

### 6
Исправил дублирование логики. Что-то изначально, когда писал, не заметил дублирование логики, так как мне было не сложно три раза написать одно и то же
```
    private static int getValueFromEnv(String nameValue, Class<?> clazz){
        String nameString = clazz.getSimpleName().toUpperCase() + "_" + nameValue;
        try{
            return Integer.parseInt(env.get(nameString));
        }catch (RuntimeException e) {
            log.error("Не удалось найти {}", nameString);
            return 0;
        }
    }

    public static int getMaxElements(Class<?> clazz){
        return getValueFromEnv("MAX_COUNT", clazz);

    }
    public static int getMaxLevel(Class<?> clazz){
        return getValueFromEnv("MAX_LEVEL", clazz);

    }
    public static int getMaxLevelsOut(Class<?> clazz){
        return getValueFromEnv("MAX_LEVELS_OUT", clazz);
    }


```

Не исправил SPR про "Классы сами лезут в конфиг". У меня это значение из конфига константное, то есть поле static. Как передавать
поле static  я не знаю. Если каждый раз передавать это поле и похожие поля в конструктор, в конструкторе образуется очень много ненужных методов, которые можно заменить константой
Класс simulation на счёт конфига я исправил и убрал "лишний конструктор", вызывая Config из Main

### 7
Position я уже сделал record ом в предыдущих пунктах.

### 8

А на счёт логов в модели... В будущем учту, что нельзя логгировать напрямую. Для этого проектa MVC пока рановат.

Типы я пока не знаю как лучше обозначить в конфиге, поэтому на данном этапе обошёлся хардкодом. 
В идеале можно опять же в Main формировать List классов и передавать его в конструктор.


На счёт getNumPriority... Мне нужно иттерироваться определённо сначала по ягодам, потом по зайцам, потом по волкам
Как по-другому такое сделать я не знаю. Даже не предполагаю.

Я конечно сделал и класс Map и класс ПоискПути, потому что Алексей мне сказал, что нужны эти классы.
Но сам я посчитал, что simulation вполне может отвечать за эти процедуры. Является ли учёбой "мне сказали сделать так, я сделал", большой вопрос. Ведь я должен дойти до решений сам.


### 9
Я просто забыл переделать конструктор вместо (x, y) на (position), а сейчас уже не хочу тратить время на исправление этого

Символы я тоже не буду исправлять сейчас, но в целом я намотал на ус, и если бы необходим был правильный результат Simulation,
а не моя учёба, то я бы запихнул символы в .env, и вызывал их из конфига. Или из конфига нельзя вызывать напрямую в коде?
Я бы тогда в Main в конструктор OutForConsole передавал бы пару ключ значение. Я уже сделал его обьектом, а не утилитой, и я бы передавал туда 
`Map<Class, String>` чтобы по ключу класса вызывать его изображение. А саму мапу формировал бы в Main.

А на счёт God class...  Я не хочу рефакторить в реальности, так как это займёт много времени, но я хочу описать логику.
Я бы просто изменил название doMove на doCycle, и в классах статичных обьектов ничего бы не делал кроме проверки не умерли ли они
А в классах Animal я бы в этом методе уже вызывал doMove

### 10
Тут уже решили, что символ должен передаваться только из Main и только в Console

### 11
В будущем учту и буду создавать класс-прокладку, передающую инфу в логгер. До этого не знал, что так нужно пользоваться, думал, просто подключил библиотеку и всё.

### 12

Вынесу в отдельный класс поиск пути и Map
Про null почитаю прежде чем убирать его из программы. Всё-таки возвращение null это явное обозначение, что ничего не вернулось.


### 13
Переименовал пакет в other. 
### 14
Про фабрику почитаю. Про паттерны я только теорию читал, поэтому когда их нужно применять на практике я не понимаю.
Переименовал название в фабрику.

### 15
Запомнил и учту в следующий раз. Я думал, раз for, то там принято i и j

### 16, 17

--

Класс Randomizer - удобная прокладка между программой и классом Random, чтобы не создавать объект рандом 1000 раз.
В видеоревью на виселицу мне именно такую прокладку и рекомендовали. 

### 18
Я удалил неиспользуемый класс первым коммитом в этой ветке.

### 19
Веб я удалил вообще, в рефакторинге он не нужен. SimulationApp оставил, удобно выводит изображение в отличие от консоли.

### 20

--


